# TODOs

If new docType is added, existing companies will not have docSettings and emailSettings for it
All dropdowns render slow. Use useMemo or move away rapidly changing stata (i.e from text input)
Create pipeline, make sure dev, stage, prod db is seeded properly, Figure out correct migration flow
